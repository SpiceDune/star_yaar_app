# Supabase – https://docs.astro.build/en/guides/backend/supabase/
# Get from https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# For auth to work, set in Dashboard → Authentication → URL Configuration:
#   Site URL: http://localhost:4321 (dev) or https://your-domain.com (prod)
#   Redirect URLs: http://localhost:4321/auth/callback and https://your-domain.com/auth/callback

# Optional: PUBLIC_* names also work (same values)
# PUBLIC_SUPABASE_URL=...
# PUBLIC_SUPABASE_ANON_KEY=...

# Direct Postgres (migrations, seeds only). Dashboard → Database → Connection string URI
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_REF.supabase.co:5432/postgres

# Google Analytics – leave empty or omit to disable
PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
